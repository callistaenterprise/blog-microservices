spring:
  application:
    name: review
  cloud:
    config:
      discovery:
        enabled: false
      uri: http://localhost:8888

#eureka.client.serviceUrl.defaultZone: http://localhost:8761/eureka/

